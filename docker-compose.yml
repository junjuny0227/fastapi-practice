services:
  api:
    build:
      context: .
      network: host    # 빌드 중 패키지 다운로드 시 호스트 네트워크 사용
    ports:
      - "80:8000"    # GSM SV HTTP 포트 → VM 80번 → 컨테이너 8000번
    restart: unless-stopped
    volumes:
      - ./board.db:/app/board.db    # DB 파일을 호스트에 유지합니다.
