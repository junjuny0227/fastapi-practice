services:
  api:
    build:
      context: .
      network: host
    ports:
      - "80:8000"
    restart: unless-stopped
    environment:
      - DATABASE_URL=sqlite:////data/board.db
    volumes:
      - board_db:/data    # Named Volume: Docker가 디렉터리를 직접 관리합니다.

volumes:
  board_db:
