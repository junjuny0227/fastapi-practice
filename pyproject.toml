[project]
name = "fastapi-practice"
version = "0.1.0"
description = "A practice project for FastAPI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "sqlalchemy>=2.0.50",
    "uvicorn[standard]>=0.48.0",
]
